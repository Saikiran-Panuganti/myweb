pipeline{
    agent any
    stages{
        stage("Git Checkout Java Project"){
            steps{
                echo "Repository checked out successfully."
                sh 'pwd'
                sh 'ls -lrt'
            }
        }
    }
}
